<?xml version="1.0" encoding="utf-8" ?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- OAuth 콜백 — Chrome Custom Tab에서 구글/카카오 로그인 완료 후 앱으로 복귀하는 경로.
                 커스텀 스킴은 도메인 검증(autoVerify) 대상이 아니다. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="gachimeokja" android:host="oauth-callback" />
            </intent-filter>

            <!-- 초대링크(App Links) — /join, /pot 경로만 좁게 열어준다. 도메인 전체를 열면
                 /reset-password 같은 링크까지 앱으로 열려 Supabase의 URL 스캔 방식과 충돌한다.
                 www/apex 둘 다 등록. assetlinks.json(www.eat-together.net/.well-known/) 검증
                 통과 전까지는 실제 기기에서 동작하지 않는다. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="www.eat-together.net" android:pathPrefix="/join" />
                <data android:scheme="https" android:host="www.eat-together.net" android:pathPrefix="/pot" />
                <data android:scheme="https" android:host="eat-together.net" android:pathPrefix="/join" />
                <data android:scheme="https" android:host="eat-together.net" android:pathPrefix="/pot" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" />
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
</manifest>
